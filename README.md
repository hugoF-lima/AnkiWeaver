
  # Flashcard Grid Webapp

  This is a code bundle for Flashcard Grid Webapp. The original project is available at https://www.figma.com/design/EEP0Xzjjlg4KXXMPvIVaMP/Flashcard-Grid-Webapp.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  